<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>GiftMatcher ✦</title>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,wght@0,700;0,900;1,700&family=DM+Sans:wght@400;500;600&display=swap" rel="stylesheet" />
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --sun: #FFD93D;
  --coral: #FF6B6B;
  --mint: #6BCB77;
  --sky: #4ECDC4;
  --lavender: #C77DFF;
  --bg: #FFFBF5;
  --white: #FFFFFF;
  --ink: #1A1A2E;
  --ink-mid: #4A4A6A;
  --ink-light: #9090B0;
  --border: #EDE8FF;
}

body {
  font-family: 'DM Sans', sans-serif;
  background: var(--bg);
  color: var(--ink);
  -webkit-font-smoothing: antialiased;
  overflow-x: hidden;
}

/* ── SQUIGGLY TOP BANNER ── */
.top-banner {
  background: var(--sun);
  padding: 10px 0;
  overflow: hidden;
  white-space: nowrap;
}
.banner-track {
  display: inline-flex; gap: 0;
  animation: marquee 18s linear infinite;
}
.banner-item {
  font-size: 12px; font-weight: 600;
  color: var(--ink); letter-spacing: 0.05em;
  padding: 0 20px;
}
.banner-sep { color: var(--coral); }
@keyframes marquee { from { transform: translateX(0); } to { transform: translateX(-50%); } }

/* ── HEADER ── */
header {
  background: var(--white);
  border-bottom: 2px solid var(--ink);
}
.header-inner {
  max-width: 1180px; margin: 0 auto;
  padding: 0 32px; height: 64px;
  display: flex; align-items: center; justify-content: space-between;
}
.logo {
  display: flex; align-items: center; gap: 10px;
  font-family: 'Fraunces', serif;
  font-size: 24px; font-weight: 900; color: var(--ink);
  letter-spacing: -0.5px;
}
.logo-blob {
  width: 36px; height: 36px;
  background: var(--coral);
  border-radius: 50% 40% 60% 40% / 40% 60% 40% 60%;
  display: flex; align-items: center; justify-content: center;
  font-size: 18px;
  animation: blob-morph 4s ease-in-out infinite;
}
@keyframes blob-morph {
  0%, 100% { border-radius: 50% 40% 60% 40% / 40% 60% 40% 60%; }
  33% { border-radius: 60% 40% 40% 60% / 50% 50% 50% 50%; }
  66% { border-radius: 40% 60% 50% 50% / 60% 40% 60% 40%; }
}
.header-chips { display: flex; gap: 8px; }
.chip {
  padding: 6px 14px; border-radius: 100px;
  font-size: 12px; font-weight: 600; border: 1.5px solid var(--ink);
  background: transparent; color: var(--ink);
}
.chip.filled { background: var(--lavender); }

/* ── HERO ── */
.hero {
  max-width: 1180px; margin: 0 auto;
  padding: 72px 32px 56px;
  position: relative;
}
.hero-tag {
  display: inline-flex; align-items: center; gap: 8px;
  background: var(--sun); color: var(--ink);
  border: 1.5px solid var(--ink); border-radius: 100px;
  padding: 6px 16px; font-size: 12px; font-weight: 600;
  margin-bottom: 28px;
}
.hero-tag-dot { width: 8px; height: 8px; background: var(--coral); border-radius: 50%; }

.hero-title {
  font-family: 'Fraunces', serif;
  font-size: clamp(50px, 7vw, 82px);
  font-weight: 900; line-height: 1.0;
  letter-spacing: -2px; margin-bottom: 28px;
  color: var(--ink);
}
.hero-title .underline-wrap { position: relative; display: inline-block; }
.hero-title .underline-wrap::after {
  content: '';
  position: absolute; left: 0; bottom: -4px; width: 100%; height: 6px;
  background: var(--sun); border-radius: 3px; z-index: -1;
}
.hero-title .pop { color: var(--coral); }
.hero-title .italic { font-style: italic; }

.hero-sub-row {
  display: flex; align-items: center; justify-content: space-between;
  gap: 40px; flex-wrap: wrap;
}
.hero-desc { font-size: 17px; color: var(--ink-mid); line-height: 1.65; max-width: 420px; }

.hero-cards-row {
  display: flex; gap: 10px; flex-shrink: 0;
}
.mini-card {
  width: 80px; height: 80px; border-radius: 16px;
  border: 2px solid var(--ink);
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  gap: 4px; background: var(--white);
  transition: transform 0.2s;
}
.mini-card:nth-child(1) { background: #FFF0F0; transform: rotate(-3deg); }
.mini-card:nth-child(2) { background: #F0FFF4; transform: rotate(1deg); }
.mini-card:nth-child(3) { background: #F0F0FF; transform: rotate(-1.5deg); }
.mini-card-icon { font-size: 26px; }
.mini-card-label { font-size: 9px; font-weight: 600; color: var(--ink-mid); text-transform: uppercase; letter-spacing: 0.05em; }

/* ── DIVIDER ── */
.wavy-divider {
  max-width: 1180px; margin: 0 auto 48px;
  padding: 0 32px;
}
.wavy-divider hr { border: none; border-top: 2px dashed #DDD8FF; }

/* ── MAIN ── */
.main-layout {
  max-width: 1180px; margin: 0 auto;
  padding: 0 32px 80px;
  display: grid; grid-template-columns: 400px 1fr; gap: 28px;
}
@media (max-width: 880px) { .main-layout { grid-template-columns: 1fr; } }

/* ── FORM CARD ── */
.form-card {
  background: var(--white);
  border: 2px solid var(--ink);
  border-radius: 24px;
  overflow: hidden;
  position: sticky; top: 20px;
  box-shadow: 6px 6px 0px var(--ink);
}
.form-card-header {
  background: var(--coral);
  padding: 24px 28px 20px;
  border-bottom: 2px solid var(--ink);
}
.form-card-title {
  font-family: 'Fraunces', serif;
  font-size: 22px; font-weight: 900; color: var(--white);
  margin-bottom: 4px;
}
.form-card-sub { font-size: 13px; color: rgba(255,255,255,0.8); }
.form-body { padding: 24px 28px 28px; }

.field { margin-bottom: 16px; }
.field-label {
  display: block; font-size: 11px; font-weight: 600;
  text-transform: uppercase; letter-spacing: 0.08em;
  color: var(--ink-mid); margin-bottom: 6px;
}
.field input, .field textarea, .field select {
  width: 100%; padding: 11px 14px;
  font-size: 14px; font-family: 'DM Sans', sans-serif;
  background: var(--bg); color: var(--ink);
  border: 1.5px solid #DDD8FF; border-radius: 12px;
  outline: none; transition: all 0.2s;
  appearance: none; -webkit-appearance: none;
}
.field input:focus, .field textarea:focus, .field select:focus {
  border-color: var(--coral); background: var(--white);
  box-shadow: 0 0 0 3px rgba(255,107,107,0.12);
}
.field input::placeholder, .field textarea::placeholder { color: #C0BBDD; }
.field textarea { resize: none; height: 76px; }

.budget-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
.sub-label { font-size: 10px; color: var(--ink-light); margin-bottom: 5px; display: block; text-transform: uppercase; letter-spacing: 0.06em; }

.btn-go {
  width: 100%; margin-top: 8px;
  background: var(--ink); color: var(--sun);
  border: 2px solid var(--ink); border-radius: 12px;
  padding: 14px 20px;
  font-family: 'Fraunces', serif; font-size: 18px; font-weight: 700;
  font-style: italic;
  cursor: pointer; display: flex; align-items: center; justify-content: center; gap: 10px;
  transition: all 0.2s;
  box-shadow: 3px 3px 0px var(--coral);
}
.btn-go:hover {
  background: var(--coral); color: var(--white);
  box-shadow: 4px 4px 0px var(--ink);
  transform: translate(-1px, -1px);
}
.btn-go:active { transform: translate(1px, 1px); box-shadow: 1px 1px 0px var(--ink); }

/* ── RESULTS ── */
.results-area { min-height: 500px; }

.empty-state {
  display: flex; flex-direction: column; align-items: center;
  justify-content: center; min-height: 480px;
  text-align: center; gap: 16px;
}
.empty-emoji {
  font-size: 72px; line-height: 1;
  animation: float 3s ease-in-out infinite;
}
@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-12px); }
}
.empty-title {
  font-family: 'Fraunces', serif;
  font-size: 26px; font-weight: 900; color: var(--ink);
}
.empty-sub { font-size: 14px; color: var(--ink-light); max-width: 240px; line-height: 1.6; }

.loading-state {
  display: flex; flex-direction: column; align-items: center;
  justify-content: center; min-height: 480px; gap: 20px;
}
.loading-emojis { font-size: 48px; animation: spin-slow 2s linear infinite; display: inline-block; }
@keyframes spin-slow { to { transform: rotate(360deg); } }
.loading-text {
  font-family: 'Fraunces', serif; font-style: italic;
  font-size: 22px; color: var(--ink-mid);
}

/* Results header */
.results-header {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 20px;
}
.results-title {
  font-family: 'Fraunces', serif; font-size: 28px; font-weight: 900; color: var(--ink);
}
.results-badge {
  background: var(--mint); color: var(--ink);
  border: 1.5px solid var(--ink); border-radius: 100px;
  padding: 4px 14px; font-size: 12px; font-weight: 600;
}

/* Gift cards */
.gift-card {
  background: var(--white);
  border: 2px solid var(--ink);
  border-radius: 20px; padding: 22px 24px;
  margin-bottom: 14px;
  display: flex; gap: 18px; align-items: flex-start;
  transition: all 0.18s;
  box-shadow: 4px 4px 0px #EDE8FF;
  position: relative;
}
.gift-card:hover {
  transform: translate(-2px, -2px);
  box-shadow: 6px 6px 0px var(--coral);
}
.card-emoji-wrap {
  width: 52px; height: 52px; flex-shrink: 0;
  border: 2px solid var(--ink); border-radius: 14px;
  display: flex; align-items: center; justify-content: center;
  font-size: 26px;
  transition: transform 0.2s;
}
.gift-card:hover .card-emoji-wrap { transform: rotate(-8deg) scale(1.1); }
.card-content { flex: 1; min-width: 0; }
.card-top-row { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; margin-bottom: 8px; }
.card-name {
  font-family: 'Fraunces', serif; font-size: 17px; font-weight: 700;
  color: var(--ink); line-height: 1.25;
}
.card-price {
  background: var(--sun); color: var(--ink);
  border: 1.5px solid var(--ink); border-radius: 100px;
  padding: 3px 12px; font-size: 12px; font-weight: 600;
  white-space: nowrap; flex-shrink: 0;
}
.card-desc { font-size: 13px; color: var(--ink-mid); line-height: 1.6; margin-bottom: 12px; }
.card-link {
  display: inline-flex; align-items: center; gap: 5px;
  font-size: 12px; font-weight: 600;
  color: var(--coral); text-decoration: none;
  border-bottom: 1.5px solid var(--coral);
  padding-bottom: 1px; transition: all 0.15s;
}
.card-link:hover { color: var(--ink); border-color: var(--ink); }

.hidden { display: none !important; }
</style>
</head>
<body>

<!-- Marquee banner -->
<div class="top-banner">
  <div class="banner-track">
    <span class="banner-item">🎁 AI-Powered Gift Ideas</span><span class="banner-item banner-sep">✦</span>
    <span class="banner-item">✨ Personalized For Your Person</span><span class="banner-item banner-sep">✦</span>
    <span class="banner-item">🎀 Every Occasion Covered</span><span class="banner-item banner-sep">✦</span>
    <span class="banner-item">💛 Thoughtful Gifts They'll Love</span><span class="banner-item banner-sep">✦</span>
    <span class="banner-item">🎁 AI-Powered Gift Ideas</span><span class="banner-item banner-sep">✦</span>
    <span class="banner-item">✨ Personalized For Your Person</span><span class="banner-item banner-sep">✦</span>
    <span class="banner-item">🎀 Every Occasion Covered</span><span class="banner-item banner-sep">✦</span>
    <span class="banner-item">💛 Thoughtful Gifts They'll Love</span><span class="banner-item banner-sep">✦</span>
  </div>
</div>

<!-- Header -->
<header>
  <div class="header-inner">
    <div class="logo">
      <div class="logo-blob">🎁</div>
      GiftMatcher
    </div>
    <div class="header-chips">
      <div class="chip">7 Picks</div>
      <div class="chip filled">Gemini AI</div>
    </div>
  </div>
</header>

<!-- Hero -->
<div class="hero">
  <div class="hero-tag">
    <div class="hero-tag-dot"></div>
    Gift discovery, made delightful
  </div>
  <h1 class="hero-title">
    The <span class="italic">happiest</span><br>
    way to find<br>
    <span class="pop underline-wrap">the perfect gift.</span>
  </h1>
  <div class="hero-sub-row">
    <p class="hero-desc">Tell us about the person you're shopping for — their age, interests, and the occasion — and our AI will surface 7 thoughtful, personalized ideas in seconds.</p>
    <div class="hero-cards-row">
      <div class="mini-card">
        <span class="mini-card-icon">🌺</span>
        <span class="mini-card-label">Mom</span>
      </div>
      <div class="mini-card">
        <span class="mini-card-icon">🎮</span>
        <span class="mini-card-label">Friend</span>
      </div>
      <div class="mini-card">
        <span class="mini-card-icon">💍</span>
        <span class="mini-card-label">Partner</span>
      </div>
    </div>
  </div>
</div>

<div class="wavy-divider"><hr /></div>

<!-- Main -->
<div class="main-layout">
  <!-- Form -->
  <div>
    <div class="form-card">
      <div class="form-card-header">
        <p class="form-card-title">Who's the lucky person? 🎉</p>
        <p class="form-card-sub">We'll find them something they'll genuinely love</p>
      </div>
      <div class="form-body">
        <div class="field">
          <label class="field-label">Their Age</label>
          <input type="number" placeholder="e.g. 28" />
        </div>
        <div class="field">
          <label class="field-label">Interests & Hobbies</label>
          <textarea placeholder="e.g. hiking, photography, cooking, vinyl records..."></textarea>
        </div>
        <div class="field">
          <label class="field-label">Occasion 🥳</label>
          <select>
            <option value="" disabled selected>What's the occasion?</option>
            <option>Birthday</option><option>Anniversary</option><option>Wedding</option>
            <option>Graduation</option><option>Baby Shower</option><option>Housewarming</option>
            <option>Retirement</option><option>Christmas</option><option>Valentine's Day</option>
            <option>Mother's Day</option><option>Father's Day</option><option>Just Because</option>
            <option>Thank You</option><option>Congratulations</option>
          </select>
        </div>
        <div class="field">
          <label class="field-label">Your Relationship</label>
          <select>
            <option value="" disabled selected>Who are they to you?</option>
            <option>Spouse/Partner</option><option>Boyfriend</option><option>Girlfriend</option>
            <option>Mother</option><option>Father</option><option>Daughter</option><option>Son</option>
            <option>Sister</option><option>Brother</option><option>Best Friend</option>
            <option>Friend</option><option>Colleague</option><option>Boss</option><option>Other</option>
          </select>
        </div>
        <div class="field">
          <label class="field-label">Budget 💸</label>
          <div class="budget-grid">
            <div>
              <span class="sub-label">Min ($)</span>
              <input type="number" placeholder="0" />
            </div>
            <div>
              <span class="sub-label">Max ($)</span>
              <input type="number" placeholder="No limit!" />
            </div>
          </div>
        </div>
        <button class="btn-go" onclick="findGifts()">
          Find Their Gift ✨
        </button>
      </div>
    </div>
  </div>

  <!-- Results -->
  <div class="results-area">
    <div class="empty-state" id="empty-state">
      <div class="empty-emoji">🎀</div>
      <p class="empty-title">Ready when you are!</p>
      <p class="empty-sub">Fill in the form and we'll come up with something they'll absolutely love.</p>
    </div>

    <div class="loading-state hidden" id="loading-state">
      <div class="loading-emojis">✨</div>
      <p class="loading-text">Finding the perfect gifts…</p>
    </div>

    <div id="gifts-list" class="hidden">
      <div class="results-header">
        <span class="results-title" id="results-title">7 Gift Ideas 🎁</span>
        <span class="results-badge">Personalized for you</span>
      </div>
      <div id="gift-cards"></div>
    </div>
  </div>
</div>

<script>
const emojis = ['🎧','☕','⌚','👨‍🍳','👜','💎','🍷'];
const mockGifts = [
  { name:"Wireless Noise-Canceling Headphones", description:"Premium over-ear headphones with active noise cancellation and 30-hour battery life. Perfect for music lovers and frequent travelers who want to stay in their own world.", priceRange:"$250–350", url:"https://www.amazon.com/s?k=wireless+noise+canceling+headphones" },
  { name:"Professional Pour-Over Coffee Set", description:"Complete pour-over brewing kit with a gooseneck kettle and ceramic dripper. Ideal for coffee enthusiasts who appreciate the craft and ritual of a perfect brew.", priceRange:"$80–120", url:"https://www.amazon.com/s?k=pour+over+coffee+set" },
  { name:"Smart Fitness Watch", description:"Advanced tracker with heart rate monitoring, GPS, sleep tracking, and smart notifications. A great companion for health-conscious individuals and athletes.", priceRange:"$200–300", url:"https://www.amazon.com/s?k=smart+fitness+watch" },
  { name:"Artisan Cooking Class Experience", description:"Hands-on workshop with a professional chef — includes ingredients, wine pairing, and recipes to take home. Memorable and genuinely different.", priceRange:"$150–200", url:"https://www.amazon.com/s?k=cooking+class+gift" },
  { name:"Premium Leather Weekender Bag", description:"Handcrafted full-grain leather bag with brass hardware and interior pockets. Built for weekend trips and getaways — gets better with every adventure.", priceRange:"$180–250", url:"https://www.amazon.com/s?k=leather+weekender+bag" },
  { name:"Personalized Engraved Jewelry Box", description:"Elegant wooden box with custom engraving and velvet-lined compartments. A timeless keepsake that holds real meaning for years to come.", priceRange:"$60–90", url:"https://www.amazon.com/s?k=personalized+jewelry+box" },
  { name:"Gourmet Food & Wine Basket", description:"Artisanal cheeses, crackers, dark chocolate, and premium wine. For the person who appreciates quality ingredients and a luxurious, celebratory moment.", priceRange:"$100–150", url:"https://www.amazon.com/s?k=gourmet+gift+basket" },
];

function findGifts() {
  document.getElementById('empty-state').classList.add('hidden');
  document.getElementById('gifts-list').classList.add('hidden');
  document.getElementById('loading-state').classList.remove('hidden');
  setTimeout(() => {
    document.getElementById('loading-state').classList.add('hidden');
    renderGifts(mockGifts);
  }, 1800);
}

function renderGifts(gifts) {
  const container = document.getElementById('gift-cards');
  container.innerHTML = '';
  gifts.forEach((g, i) => {
    const card = document.createElement('div');
    card.className = 'gift-card';
    card.innerHTML = `
      <div class="card-emoji-wrap">${emojis[i] || '🎁'}</div>
      <div class="card-content">
        <div class="card-top-row">
          <p class="card-name">${g.name}</p>
          <span class="card-price">${g.priceRange}</span>
        </div>
        <p class="card-desc">${g.description}</p>
        <a href="${g.url}" target="_blank" class="card-link">Shop on Amazon ↗</a>
      </div>
    `;
    container.appendChild(card);
  });
  document.getElementById('gifts-list').classList.remove('hidden');
}
</script>
</body>
</html>
